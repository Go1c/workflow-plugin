---
description: 把想法或文档规划成可执行的 Workflow 需求蓝图，并可在独立授权后落单
---

强制调起 **workflow-planning** 技能处理下面的输入。先读取来源与项目上下文，一次只问一个真正改变蓝图的问题，再展示完整蓝图。蓝图内容确认不等于线上写入授权；用户只要方案时到此停止。写入前必须另行展示目标 Workflow 项目、蓝图修订号、查重结果和准确对象数量，并取得针对该范围的明确落单授权。

规划与落单阶段都不启动实现、不创建 WorkItem 或 Worktree、不运行目标仓库测试，也不流转需求状态。

用户输入：

$ARGUMENTS
