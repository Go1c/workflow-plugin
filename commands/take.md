---
description: 以执行者身份承接一张 Workflow 单：读单核对、流转开工、完成后回写证据并交回
---

调起 **workflow-execute** 技能承接下面的单。先判定执行模式（自持凭证直连，或无凭证由调度方代写——当前目录没有 `.workflow` 绑定时禁止靠全局 profile 兜底写数据）；读单按「正文 + 评论 + 附件 + 验收项」拉全，前置未满足就停下报告；**开工前先梳理需求，把歧义与需要拍板的决策点连同建议一并摆出来讨论，全部有答复才现查 transitions 流转到进行中**；干活阶段按讨论确认的拆分尽可能并行子 Agent（互斥所有权切分，子 Agent 不碰凭证与回写）；完成后按统一证据评论模板回写（附件 → 评论 → 流转待验收，每步读回），再按交回格式汇报。

预设边界：**只承接指定的单** —— 不建新单、不动别人的卡、不改原始描述、不验收自己的交付、不跳过讨论直接开工。

单号或派遣内容：

$ARGUMENTS
